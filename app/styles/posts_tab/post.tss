'#mainTable[platform=ios]' : {
  separatorStyle: Ti.UI.iPhone.ListViewSeparatorStyle.NONE
}
'.shareButton' : {
  title: L('shareButton'),
  image: '/images/share.png',
  top: -25,
  width: 50
}
'#featuredImageView' : {
  left: 0,
  right: 0,
  height: 200
}
'#featuredImageView[platform=android]' : {
  scrollingEnabled: false,
  contentWidth: '100%',
  scrollType: 'vertical'
}
'.leftBorder' : {
  left: 0,
  width: 4,
  height: 95,
  backgroundColor: Alloy.CFG.style.mainColor
}
'.datePrettyLabel' : {
  left: 17,
  top: 0,
  color: '#979797'
}
'.titleLabel' : {
  left: 17,
  right: 17,
  height: 70,
  bottom: 0,
  font: {
    fontSize: 20
  }
}
'.contentLabel' : {
  left: 15,
  right: 15,
  top: 15,
  bottom: 15,
  color: 'gray',
  font: {
    fontSize: 14
  }
}
